import { DomainPipePipe } from './domain-pipe.pipe';

describe('DomainPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DomainPipePipe();
    expect(pipe).toBeTruthy();
  });
});
